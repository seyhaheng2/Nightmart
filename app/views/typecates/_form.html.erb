<%= form_for(@typecate) do |f| %>
  <% if @typecate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@typecate.errors.count, "error") %> prohibited this typecate from being saved:</h2>

      <ul>
      <% @typecate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
